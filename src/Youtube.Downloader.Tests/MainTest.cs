using System.Collections.Generic;
using NUnit.Framework;
using Youtube.Downloader.Loaders;

namespace Youtube.Downloader.Tests
{
    [TestFixture]
    public class MainTest
    {
        [Test]
        public void Test1() {
            /*var httpLoader = new HttpLoader();

            var videoUrl = "";
            var id = UrlsComposer.ParseId(videoUrl);
            var infoPage = new InfoPage(httpLoader, id);

            var title = infoPage.GetTitle();
            var videoUrls = infoPage.GetVideoUrls();

            foreach (var keyValuePair in videoUrls) {
                //Format.Create(keyValuePair);
            }*/
        }
    }
}